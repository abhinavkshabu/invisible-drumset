# Server-side (launcher.py) uses only Python standard library.
# The packages below are needed only if running app.py locally.
# They are kept commented for reference.
# opencv-python-headless>=4.10.0.84
# opencv-contrib-python-headless>=4.10.0.84
# mediapipe>=0.10.30
# pyautogui>=0.9.54
# numpy>=1.26.4
# pynput>=1.7.6
# pygame>=2.5.0
